# Frontend Environment Variables
# Local dev: leave VITE_API_URL empty — Vite proxy handles /api → localhost:8000
# Production (Railway): set to your backend Railway URL (no trailing slash)
VITE_API_URL=
